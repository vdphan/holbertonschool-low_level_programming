#include "holberton.h"
/**
 * _isdigit - Returns 1 if c is a digit. Otherwise, returns 0.
 *@c: integer value
 *
 * Return: Always 0.
 */
int _isdigit(int c)
{
	if ((c >= '0') && (c <= '9'))
		return (1);
	else
		return (0);
	return (0);
}
